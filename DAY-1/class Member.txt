class Member:
	attributes:
		name , id,membershipDate , membership_duration_in_years
	
	constructor(name , id,membershipDate , membership_duration_in_years)
		SET self.name = name
        	SET self.member_id = member_id
        	SET self.membership_date = membership_date
        	SET self.membership_duration_in_years = membership_duration_in_years
		
	
	method checkMembershipValidity( curr_date):
		SET expiry_date = membership_date + membership_duration_years
		IF curr_date <= expiry_date
			RETURN "Membership is valid"
        	ELSE:
            		RETURN "Membership has expired"
	
	
	Method Display_member_info():
			 PRINT "Member ID: ", member_id
       		 PRINT "Membership Start Date: ", membership_date
       		 PRINT "Membership Duration (Years): ", membership_duration_in_years 


class main:
	CREATE member1 = Member("Alice Smith", 101, "2021-07-01", 3)

	SET today_date = "2025-08-11"
	PRINT member1.check_membership_validity(today_date)

	member1.display_member_info()
