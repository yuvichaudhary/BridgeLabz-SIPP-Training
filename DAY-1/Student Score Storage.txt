class Student:
	MAP studentScore(key : String , value:Integer)
	ADD(Abhay , 95) to studentScore
	ADD(sumit , 98) to studentScore
	ADD(Abhi , 99) to studentScore

	Attribute highscore = 0
	Attribute topScrorer = ""
	
	FOR EACH (Name,Score) in studentScore:
	 IF score > highestScore THEN
        SET highestScore = score
        SET topScorer = name

PRINT "Top Scorer: " + topScorer