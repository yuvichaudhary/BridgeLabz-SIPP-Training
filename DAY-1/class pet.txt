class pet :
	attribute name,
	attribute age,
	attribute breed,
	attribute color
	constructor(name , age , breed , color):
	set age , name , breed ,color

	Method display_info():
        Print "Name: " + name
        Print "Age: " + age
        Print "Breed: " + breed
        Print "Color: " + color
	

class dog inherits pet:
	attribute is_trained
	constructor(name , age , breed , color,is_trained):
		call super(name , age, breed,color)
		set is_trained

	Method display_info():
        Call super.display_info()
        Print "Trained: " + is_trained

class Cat inherits pet:
	attribute is_domestic;
	constructor(name , age , breed , color , is_domestic):
		call super(name ,age, breed ,color)
		set is_domestic


	Method display_info():
        Call super.display_info()
        Print "Trained: " + is_domestic

class Bird inherits pet:
	attribute is_speak
	constructor(name , age, breed,color):
		call super(name , age , bree , color)
		set is_speak
	
	Method display_info():
        Call super.display_info()
        Print "Trained: " + is_speak
	


	
	